# Torch ORM

一个轻量级的 ORM 库，支持浏览器和 Node.js 环境。基于适配器模式设计，可以轻松扩展支持不同的存储后端。

## 特性

- 支持浏览器和 Node.js
- 基于适配器模式，易于扩展
- TypeScript 支持
- 简单的 API 设计

# 编码标准

- 采取最佳最简洁的实现
- 不要添加额外的中文注释，以免显得代码过于臃肿
- 遵循prettier默认的代码风格
- 保持原有的代码目录结构
- 修改完成之后需要运行测试并保证测试通过

# 命令行要求

- 运行测试时请不要打断测试

# 代码风格

- 按照prettier的默认风格，使用双引号，文件末尾额外换行